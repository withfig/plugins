[plugin]
name = "replay.fish"
type = "shell"
description = "Run Bash commands replaying changes in Fish."
github = "jorgebucaran/replay.fish"
license = ["MIT"]
shells = ["fish"]
tags = ["fish", "bash", "env"]

[installation.shell]
source = { github = true }
