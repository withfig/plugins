[plugin]
name = "autopair.fish"
type = "shell"
description = "Auto-complete matching pairs in the Fish command line."
github = "jorgebucaran/autopair.fish"
license = ["MIT"]
shells = ["fish"]
tags = ["fish", "commandline", "fish-plugin"]

[installation.shell]
source = { github = true }
